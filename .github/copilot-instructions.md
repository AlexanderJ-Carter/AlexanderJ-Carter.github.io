# GitHub Copilot

Primary site: **Folio** (`folio/`). Pages: `gateway/`.

Edit `folio/src/**`. See `CLAUDE.md` and `docs/FOLIO.md`. Do not push unless asked. Never commit secrets or delete Folio security assets.
