# GitHub Copilot

Primary site: **Folio** (`folio/` → www). Pages: `gateway/`. Legacy Astro: `src/` (migration only).

Edit `folio/src/**`. See `CLAUDE.md` and `docs/FOLIO-CUTOVER.md`. Do not push unless asked. Never commit secrets or delete Folio security assets.
