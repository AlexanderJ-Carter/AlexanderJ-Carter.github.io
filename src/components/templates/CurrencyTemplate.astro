---
import BaseLayout from '../../layouts/BaseLayout.astro';
import RelatedTools from '../RelatedTools.astro';
import PageHero from '../PageHero.astro';

const { lang } = Astro.props;

type Lang = 'zh-CN' | 'zh-TW' | 'en-GB' | 'fr' | 'ru';

const currencyData: Record<Lang, any> = {
  'zh-CN': {
    title: '智能汇率工具',
    headerTitle: '💱 智能汇率工具',
    headerDesc: '实时汇率查询 · 自定义汇率计算 · 结售汇小助手',
    mainRatesTitle: '主要货币汇率',
    subTitle: '今日参考汇率（1单位外币 = ? 人民币）',
    disclaimer: '仅供参考，不作为交易或结售汇依据。实际以银行/机构为准。',
    refresh: '刷新',
    refreshing: '更新中...',
    source: '数据源',
    converterTitle: '💰 汇率计算器',
    converterDesc: '支持实时汇率和自定义汇率计算',
    amount: '金额',
    from: '从',
    to: '到',
    customRate: '自定义汇率',
    customRatePlaceholder: '留空使用实时汇率',
    customRateHint: '例如：银行牌价、结售汇价格等',
    result: '计算结果',
    convert: '立即计算',
    useRealRate: '使用实时汇率',
    currentRate: '当前汇率',
    loading: '加载中...',
    updated: '更新时间',
    justNow: '刚刚',
    minutesAgo: '分钟前',
    hoursAgo: '小时前',
    errorLoading: '⚠️ 加载失败，请稍后重试',
    calculating: '计算中...',
    sameCurrency: '相同货币，无需转换',
    usingCustomRate: '使用自定义汇率',
    usingRealRateStatus: '使用实时汇率',
    errorFetching: '无法获取汇率',
    convertError: '转换失败',
    networkError: '请检查网络连接或稍后重试',
    tipsTitle: '💡 结售汇小贴士',
    tipsDesc: '实用建议，助您更好地管理外汇',
    tips: [
      {
        icon: '🏦',
        title: '银行牌价差异',
        desc: '各银行现钞买入价和现汇买入价通常不同，现钞价会略低，建议提前对比。',
      },
      {
        icon: '⏰',
        title: '最佳兑换时机',
        desc: '汇率波动较大时，可分批兑换降低风险。出国前1-2周关注汇率走势为佳。',
      },
      {
        icon: '📱',
        title: '手机银行方便快捷',
        desc: '使用手机银行结售汇通常手续费更低，且可以7×24小时操作，到账快。',
      },
      {
        icon: '💳',
        title: '境外消费建议',
        desc: '信用卡境外消费通常按当日汇率结算，小额高频消费可能产生额外手续费。',
      },
    ],
  },
  'zh-TW': {
    title: '智能匯率工具',
    headerTitle: '💱 智能匯率工具',
    headerDesc: '即時匯率查詢 · 自定義匯率計算 · 結售匯小幫手',
    mainRatesTitle: '主要貨幣匯率',
    subTitle: '今日參考匯率（1單位外幣 = ? 人民幣）',
    disclaimer: '僅供參考，不作為交易或結售匯依據。實際以銀行/機構為準。',
    refresh: '重新整理',
    refreshing: '更新中...',
    source: '資料來源',
    converterTitle: '💰 匯率計算機',
    converterDesc: '支援即時匯率和自定義匯率計算',
    amount: '金額',
    from: '從',
    to: '到',
    customRate: '自定義匯率',
    customRatePlaceholder: '留空使用即時匯率',
    customRateHint: '例如：銀行牌價、結售匯價格等',
    result: '計算結果',
    convert: '立即計算',
    useRealRate: '使用即時匯率',
    currentRate: '當前匯率',
    loading: '載入中...',
    updated: '更新時間',
    justNow: '剛剛',
    minutesAgo: '分鐘前',
    hoursAgo: '小時前',
    errorLoading: '⚠️ 載入失敗，請稍後重試',
    calculating: '計算中...',
    sameCurrency: '相同貨幣，無需轉換',
    usingCustomRate: '使用自定義匯率',
    usingRealRateStatus: '使用即時匯率',
    errorFetching: '無法獲取匯率',
    convertError: '轉換失敗',
    networkError: '請檢查網路連線或稍後重試',
    tipsTitle: '💡 結售匯小撇步',
    tipsDesc: '實用建議，助您更好地管理外匯',
    tips: [
      {
        icon: '🏦',
        title: '銀行牌價差異',
        desc: '各銀行現鈔買入價和現匯買入價通常不同，現鈔價會略低，建議提前比價。',
      },
      {
        icon: '⏰',
        title: '最佳兌換時機',
        desc: '匯率波動較大時，可分批兌換降低風險。出國前1-2週關注匯率走勢為佳。',
      },
      {
        icon: '📱',
        title: '手機銀行方便快捷',
        desc: '使用手機銀行結售匯通常手續費更低，且可以7×24小時操作，到帳快。',
      },
      {
        icon: '💳',
        title: '境外消費建議',
        desc: '信用卡境外消費通常按當日匯率結算，小額高頻消費可能產生額外手續費。',
      },
    ],
  },
  'en-GB': {
    title: 'Smart Currency Tool',
    headerTitle: '💱 Smart Currency Tool',
    headerDesc: 'Real-time Rates · Custom Rate Calculator · Exchange Helper',
    mainRatesTitle: 'Major Exchange Rates',
    subTitle: "Today's Reference Rates (1 foreign currency = ? CNY)",
    disclaimer:
      'For reference only. Not for trading or exchange. Check your bank for actual rates.',
    refresh: 'Refresh',
    refreshing: 'Updating...',
    source: 'Data Source',
    converterTitle: '💰 Currency Converter',
    converterDesc: 'Supports real-time and custom rate calculations',
    amount: 'Amount',
    from: 'From',
    to: 'To',
    customRate: 'Custom Rate',
    customRatePlaceholder: 'Leave empty for real-time rate',
    customRateHint: 'E.g.: Bank rate, exchange counter price',
    result: 'Result',
    convert: 'Calculate',
    useRealRate: 'Use Real-time Rate',
    currentRate: 'Current Rate',
    loading: 'Loading...',
    updated: 'Updated',
    justNow: 'Just now',
    minutesAgo: 'min ago',
    hoursAgo: 'hrs ago',
    errorLoading: '⚠️ Failed to load, please try again later',
    calculating: 'Calculating...',
    sameCurrency: 'Same currency, no conversion needed',
    usingCustomRate: 'Using custom rate',
    usingRealRateStatus: 'Using real-time rate',
    errorFetching: 'Unable to fetch rate',
    convertError: 'Conversion failed',
    networkError: 'Please check your connection or try again later',
    tipsTitle: '💡 Exchange Tips',
    tipsDesc: 'Practical advice to help you manage foreign exchange better',
    tips: [
      {
        icon: '🏦',
        title: 'Bank Rate Differences',
        desc: 'Cash buying rates and transfer buying rates differ. Cash rates are usually lower.',
      },
      {
        icon: '⏰',
        title: 'Best Time to Exchange',
        desc: 'Monitor rates 1-2 weeks before travel. Split large exchanges to reduce risk.',
      },
      {
        icon: '📱',
        title: 'Mobile Banking Benefits',
        desc: 'Lower fees and 24/7 availability. Most transactions complete instantly.',
      },
      {
        icon: '💳',
        title: 'Overseas Spending',
        desc: 'Credit cards use daily rates. Watch for fees on small frequent transactions.',
      },
    ],
  },
  fr: {
    title: 'Outil de change intelligent',
    headerTitle: '💱 Outil de change intelligent',
    headerDesc:
      'Taux en temps réel · Calculateur personnalisé · Assistant de change',
    mainRatesTitle: 'Taux de change principaux',
    subTitle: 'Taux de référence du jour (1 devise = ? CNY)',
    disclaimer:
      'À titre indicatif uniquement. Consultez votre banque pour les taux réels.',
    refresh: 'Actualiser',
    refreshing: 'Mise à jour...',
    source: 'Source de données',
    converterTitle: '💰 Convertisseur de devises',
    converterDesc: 'Prend en charge les calculs en temps réel et personnalisés',
    amount: 'Montant',
    from: 'De',
    to: 'À',
    customRate: 'Taux personnalisé',
    customRatePlaceholder: 'Laissez vide pour le taux en temps réel',
    customRateHint: 'Ex : Taux bancaire, prix du guichet',
    result: 'Résultat',
    convert: 'Calculer',
    useRealRate: 'Utiliser le taux en temps réel',
    currentRate: 'Taux actuel',
    loading: 'Chargement...',
    updated: 'Mis à jour',
    justNow: "À l'instant",
    minutesAgo: 'min',
    hoursAgo: 'h',
    errorLoading: '⚠️ Échec du chargement, veuillez réessayer plus tard',
    calculating: 'Calcul en cours...',
    sameCurrency: 'Même devise, aucune conversion nécessaire',
    usingCustomRate: 'Utilisation du taux personnalisé',
    usingRealRateStatus: 'Utilisation du taux en temps réel',
    errorFetching: 'Impossible de récupérer le taux',
    convertError: 'Échec de la conversion',
    networkError: 'Veuillez vérifier votre connexion ou réessayer plus tard',
    tipsTitle: '💡 Conseils de change',
    tipsDesc: 'Conseils pratiques pour mieux gérer vos devises',
    tips: [
      {
        icon: '🏦',
        title: 'Différences bancaires',
        desc: "Les taux d'achat en espèces et virements diffèrent. Les espèces sont généralement plus bas.",
      },
      {
        icon: '⏰',
        title: 'Meilleur moment pour échanger',
        desc: 'Surveillez les taux 1-2 semaines avant le voyage. Divisez les gros montants.',
      },
      {
        icon: '📱',
        title: 'Avantages bancaires mobiles',
        desc: 'Frais réduits et disponibilité 24h/24. La plupart des transactions sont instantanées.',
      },
      {
        icon: '💳',
        title: "Dépenses à l'étranger",
        desc: 'Les cartes utilisent les taux quotidiens. Attention aux frais sur petites transactions.',
      },
    ],
  },
  ru: {
    title: 'Умный валютный инструмент',
    headerTitle: '💱 Умный валютный инструмент',
    headerDesc:
      'Курсы в реальном времени · Пользовательский калькулятор · Помощник обмена',
    mainRatesTitle: 'Основные курсы валют',
    subTitle: 'Референсные курсы сегодня (1 валюта = ? CNY)',
    disclaimer:
      'Только для справки. Не для сделок. Актуальные курсы уточняйте в банке.',
    refresh: 'Обновить',
    refreshing: 'Обновление...',
    source: 'Источник данных',
    converterTitle: '💰 Конвертер валют',
    converterDesc:
      'Поддержка расчетов в реальном времени и пользовательских курсов',
    amount: 'Сумма',
    from: 'Из',
    to: 'В',
    customRate: 'Пользовательский курс',
    customRatePlaceholder: 'Оставьте пустым для текущего курса',
    customRateHint: 'Например: Банковский курс, цена обменника',
    result: 'Результат',
    convert: 'Рассчитать',
    useRealRate: 'Использовать текущий курс',
    currentRate: 'Текущий курс',
    loading: 'Загрузка...',
    updated: 'Обновлено',
    justNow: 'Только что',
    minutesAgo: 'мин назад',
    hoursAgo: 'ч назад',
    errorLoading: '⚠️ Ошибка загрузки, попробуйте позже',
    calculating: 'Вычисление...',
    sameCurrency: 'Та же валюта, конвертация не требуется',
    usingCustomRate: 'Используется пользовательский курс',
    usingRealRateStatus: 'Используется текущий курс',
    errorFetching: 'Не удалось получить курс',
    convertError: 'Ошибка конвертации',
    networkError: 'Проверьте подключение или попробуйте позже',
    tipsTitle: '💡 Советы по обмену',
    tipsDesc: 'Практические советы для лучшего управления валютой',
    tips: [
      {
        icon: '🏦',
        title: 'Разница банковских курсов',
        desc: 'Курсы покупки наличных и переводов различаются. Наличные обычно ниже.',
      },
      {
        icon: '⏰',
        title: 'Лучшее время обмена',
        desc: 'Следите за курсами за 1-2 недели до поездки. Разделяйте крупные суммы.',
      },
      {
        icon: '📱',
        title: 'Преимущества мобильного банка',
        desc: 'Более низкие комиссии и доступность 24/7. Большинство операций мгновенны.',
      },
      {
        icon: '💳',
        title: 'Траты за границей',
        desc: 'Карты используют дневные курсы. Остерегайтесь комиссий за мелкие операции.',
      },
    ],
  },
};

const content = currencyData[lang as Lang] || currencyData['en-GB'];
---

<BaseLayout title={`${content.title} - Alexander Carter`}>
  <PageHero title={content.title} description={content.headerDesc} />

  <!-- Currency Rates Section -->
  <section class="py-16 container mx-auto px-4 lg:px-8">
    <!-- Main Currency Rates -->
    <div class="mb-16">
      <div
        class="flex flex-col md:flex-row md:items-start md:justify-between gap-4 mb-6"
      >
        <div>
          <h2 class="text-3xl font-bold mb-2">
            {content.mainRatesTitle}
          </h2>
          <p class="text-[rgb(var(--color-text-secondary))] mb-1">
            {content.subTitle}
          </p>
          {
            content.disclaimer && (
              <p class="text-xs text-[rgb(var(--color-text-tertiary))] mb-2">
                {content.disclaimer}
              </p>
            )
          }
          <p
            class="text-sm text-[rgb(var(--color-text-tertiary))] flex items-center gap-2"
            id="rates-updated"
          >
            <svg
              class="w-4 h-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span id="update-time-text">{content.loading}</span>
          </p>
        </div>
        <button
          id="refresh-rates"
          class="px-6 py-3 rounded-lg border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] text-[rgb(var(--color-text-primary))] font-semibold hover:bg-[rgb(var(--color-bg-secondary))] transition-colors flex items-center gap-2"
        >
          <svg
            id="refresh-icon"
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"
            ></path>
          </svg>
          <span id="refresh-text">{content.refresh}</span>
        </button>
      </div>
      <div
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6"
        id="rates-grid"
      >
        <!-- Rates will be inserted here -->
        <div
          class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-6 animate-pulse rounded-xl"
        >
          <div class="h-6 bg-gray-200 dark:bg-gray-700 rounded w-1/2 mb-4">
          </div>
          <div class="h-8 bg-gray-200 dark:bg-gray-700 rounded w-3/4"></div>
        </div>
      </div>
      <p
        class="text-xs text-[rgb(var(--color-text-tertiary))] mt-4 text-center"
      >
        {content.source}: <a
          href="https://open.er-api.com"
          target="_blank"
          class="text-primary-500 hover:underline">open.er-api.com</a
        >
      </p>
    </div>

    <!-- Converter -->
    <div class="max-w-3xl mx-auto mb-16">
      <div
        class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-8 md:p-10 rounded-xl"
      >
        <div class="mb-6">
          <h3 class="text-2xl font-heading font-bold mb-2">
            {content.converterTitle.replace(/^💰\s*/, '')}
          </h3>
          <p class="text-[rgb(var(--color-text-secondary))]">
            {content.converterDesc}
          </p>
        </div>
        <form id="converter-form" class="space-y-6">
          <div>
            <label
              class="block text-sm font-medium mb-2 text-[rgb(var(--color-text-primary))]"
              >{content.amount}</label
            >
            <input
              type="number"
              id="amount"
              value="100"
              min="0"
              step="0.01"
              placeholder="100"
              class="w-full px-4 py-3 rounded-lg bg-[rgb(var(--color-bg-secondary))] border-2 border-[rgb(var(--color-border))] text-[rgb(var(--color-text-primary))] text-lg focus:border-primary-500 focus:outline-none transition-colors"
            />
          </div>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <label
                class="block text-sm font-medium mb-2 text-[rgb(var(--color-text-primary))]"
                >{content.from}</label
              >
              <select
                id="from-currency"
                class="w-full px-4 py-3 rounded-lg bg-[rgb(var(--color-bg-secondary))] border-2 border-[rgb(var(--color-border))] text-[rgb(var(--color-text-primary))] focus:border-primary-500 focus:outline-none transition-colors"
              >
                <option value="CNY" selected>CNY - 人民币 ¥</option>
                <option value="USD">USD - 美元 $</option>
                <option value="EUR">EUR - 欧元 €</option>
                <option value="GBP">GBP - 英镑 £</option>
                <option value="JPY">JPY - 日元 ¥</option>
                <option value="HKD">HKD - 港币 HK$</option>
                <option value="AUD">AUD - 澳元 A$</option>
                <option value="CAD">CAD - 加元 C$</option>
                <option value="SGD">SGD - 新元 S$</option>
              </select>
            </div>
            <div>
              <label
                class="block text-sm font-medium mb-2 text-[rgb(var(--color-text-primary))]"
                >{content.to}</label
              >
              <select
                id="to-currency"
                class="w-full px-4 py-3 rounded-lg bg-[rgb(var(--color-bg-secondary))] border-2 border-[rgb(var(--color-border))] text-[rgb(var(--color-text-primary))] focus:border-primary-500 focus:outline-none transition-colors"
              >
                <option value="USD" selected>USD - 美元 $</option>
                <option value="EUR">EUR - 欧元 €</option>
                <option value="GBP">GBP - 英镑 £</option>
                <option value="JPY">JPY - 日元 ¥</option>
                <option value="HKD">HKD - 港币 HK$</option>
                <option value="CNY">CNY - 人民币 ¥</option>
                <option value="AUD">AUD - 澳元 A$</option>
                <option value="CAD">CAD - 加元 C$</option>
                <option value="SGD">SGD - 新元 S$</option>
              </select>
            </div>
          </div>

          <!-- Custom Rate Input -->
          <div class="border-t border-[rgb(var(--color-border))] pt-6">
            <div class="flex items-center justify-between mb-2">
              <label
                class="block text-sm font-medium text-[rgb(var(--color-text-primary))]"
                >{content.customRate}</label
              >
              <button
                type="button"
                id="use-real-rate-btn"
                class="text-xs text-primary-500 hover:text-primary-600 font-medium"
              >
                {content.useRealRate}
              </button>
            </div>
            <input
              type="number"
              id="custom-rate"
              placeholder={content.customRatePlaceholder}
              step="0.0001"
              min="0"
              class="w-full px-4 py-3 rounded-lg bg-[rgb(var(--color-bg-secondary))] border-2 border-[rgb(var(--color-border))] text-[rgb(var(--color-text-primary))] focus:border-accent-500 focus:outline-none transition-colors"
            />
            <p
              class="text-xs text-[rgb(var(--color-text-tertiary))] mt-1 flex items-center gap-1"
            >
              <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                <path
                  fill-rule="evenodd"
                  d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z"
                  clip-rule="evenodd"></path>
              </svg>
              {content.customRateHint}
            </p>
            <p
              id="current-rate-display"
              class="text-sm text-[rgb(var(--color-text-secondary))] mt-2"
            >
            </p>
          </div>

          <button
            type="button"
            id="convert-btn"
            class="w-full py-4 border border-[rgb(var(--color-text-primary))] bg-[rgb(var(--color-text-primary))] text-[rgb(var(--color-bg-primary))] rounded-lg hover:opacity-90 transition-opacity font-bold text-lg flex items-center justify-center gap-2"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"
              ></path>
            </svg>
            {content.convert}
          </button>
          <div
            class="mt-6 p-8 bg-[rgb(var(--color-bg-secondary))] rounded-xl text-center border border-[rgb(var(--color-border))]"
          >
            <p class="text-sm text-[rgb(var(--color-text-secondary))] mb-3">
              {content.result}
            </p>
            <p
              id="conversion-result"
              class="text-4xl md:text-5xl font-bold text-[rgb(var(--color-text-primary))]"
            >
              --
            </p>
            <p
              id="rate-used"
              class="text-xs text-[rgb(var(--color-text-tertiary))] mt-3"
            >
            </p>
          </div>
        </form>
      </div>
    </div>

    <!-- Tips Section -->
    <div class="max-w-4xl mx-auto">
      <div class="mb-8">
        <h3 class="text-2xl font-heading font-bold mb-2">
          {content.tipsTitle.replace(/^💡\s*/, '')}
        </h3>
        <p class="text-[rgb(var(--color-text-secondary))]">
          {content.tipsDesc}
        </p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {
          content.tips.map((tip: any) => (
            <div class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-6 rounded-xl">
              <div class="flex items-start gap-4">
                <div class="text-4xl flex-shrink-0 group-hover:scale-110 transition-transform">
                  {tip.icon}
                </div>
                <div>
                  <h4 class="text-lg font-bold mb-2 text-[rgb(var(--color-text-primary))] group-hover:text-primary-500 transition-colors">
                    {tip.title}
                  </h4>
                  <p class="text-sm text-[rgb(var(--color-text-secondary))] leading-relaxed">
                    {tip.desc}
                  </p>
                </div>
              </div>
            </div>
          ))
        }
      </div>
    </div>

    <div class="container mx-auto px-4 lg:px-8 max-w-4xl mt-16">
      <RelatedTools lang={lang} current="currency" />
    </div>
  </section>
</BaseLayout>

<script is:inline define:vars={{ content, lang }}>
  const amountInput = document.getElementById('amount');
  const fromSelect = document.getElementById('from-currency');
  const toSelect = document.getElementById('to-currency');
  const customRateInput = document.getElementById('custom-rate');
  const resultElement = document.getElementById('conversion-result');
  const rateUsedElement = document.getElementById('rate-used');
  const currentRateDisplay = document.getElementById('current-rate-display');
  const convertBtn = document.getElementById('convert-btn');
  const useRealRateBtn = document.getElementById('use-real-rate-btn');
  const ratesGrid = document.getElementById('rates-grid');
  const updateTimeText = document.getElementById('update-time-text');
  const refreshBtn = document.getElementById('refresh-rates');
  const refreshIcon = document.getElementById('refresh-icon');
  const refreshText = document.getElementById('refresh-text');

  // Store last update time
  let lastUpdateTime = null;

  // Major currencies to display
  const majorCurrencies = [
    'USD',
    'EUR',
    'GBP',
    'JPY',
    'HKD',
    'AUD',
    'CAD',
    'SGD',
  ];

  // Currency symbols
  const currencySymbols = {
    USD: '$',
    EUR: '€',
    GBP: '£',
    JPY: '¥',
    CNY: '¥',
    HKD: 'HK$',
    AUD: 'A$',
    CAD: 'C$',
    SGD: 'S$',
  };

  function getTimeAgo(date) {
    const now = new Date();
    const diffMs = now - date;
    const diffMins = Math.floor(diffMs / 60000);
    const diffHours = Math.floor(diffMs / 3600000);

    if (diffMins < 1) return content.justNow;
    if (diffMins < 60) return `${diffMins} ${content.minutesAgo}`;
    if (diffHours < 24) return `${diffHours} ${content.hoursAgo}`;
    return date.toLocaleString(lang, {
      month: 'short',
      day: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
    });
  }

  function updateTimeDisplay() {
    if (lastUpdateTime && updateTimeText) {
      updateTimeText.textContent = getTimeAgo(lastUpdateTime);
    }
  }

  async function fetchRates() {
    if (!ratesGrid) return;

    // Update button state
    if (refreshBtn && refreshIcon && refreshText) {
      refreshBtn.disabled = true;
      refreshIcon.classList.add('animate-spin');
      refreshText.textContent = content.refreshing;
    }

    ratesGrid.innerHTML = `<div class="col-span-full text-center py-8 text-[rgb(var(--color-text-secondary))]"><svg class="animate-spin h-8 w-8 mx-auto mb-2" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>${content.loading}</div>`;

    try {
      const response = await fetch('https://open.er-api.com/v6/latest/CNY');
      const data = await response.json();

      if (data.result === 'success' && data.rates) {
        lastUpdateTime = new Date(data.time_last_update_unix * 1000);

        ratesGrid.innerHTML = '';
        updateTimeDisplay();

        majorCurrencies.forEach((currency) => {
          const rate = data.rates[currency];
          if (rate) {
            const inverseRate = (1 / rate).toFixed(4);
            const symbol = currencySymbols[currency] || currency;

            const card = document.createElement('div');
            card.className =
              'border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-6 rounded-xl hover:bg-[rgb(var(--color-bg-secondary))] transition-colors cursor-pointer group';
            card.innerHTML = `
                            <div class="flex justify-between items-start mb-3">
                                <div>
                                    <span class="text-2xl font-bold text-[rgb(var(--color-text-primary))]">${currency}</span>
                                    <p class="text-xs text-[rgb(var(--color-text-tertiary))] mt-1">1 ${currency}</p>
                                </div>
                                <span class="text-3xl opacity-50 group-hover:opacity-100 transition-opacity">${symbol}</span>
                            </div>
                            <div class="text-3xl font-bold text-[rgb(var(--color-text-primary))]">
                                ¥${inverseRate}
                            </div>
                        `;
            ratesGrid.appendChild(card);
          }
        });
      } else {
        throw new Error('API returned no data');
      }
    } catch (error) {
      console.error('Error fetching rates:', error);
      ratesGrid.innerHTML = `<div class="col-span-full text-center py-8 text-red-500">${content.errorLoading}</div>`;
    } finally {
      // Reset button state
      if (refreshBtn && refreshIcon && refreshText) {
        refreshBtn.disabled = false;
        refreshIcon.classList.remove('animate-spin');
        refreshText.textContent = content.refresh;
      }
    }
  }

  async function convertCurrency() {
    const amount = parseFloat(amountInput.value);
    const from = fromSelect.value;
    const to = toSelect.value;
    const customRate = parseFloat(customRateInput.value);

    if (!amount || !from || !to || isNaN(amount)) {
      resultElement.textContent = '--';
      return;
    }

    resultElement.textContent = content.calculating;

    try {
      if (from === to) {
        const symbol = currencySymbols[to] || '';
        resultElement.textContent = `${symbol}${amount.toFixed(2)}`;
        rateUsedElement.textContent = content.sameCurrency;
        return;
      }

      let rate;
      let rateSource = '';

      // Use custom rate if provided
      if (!isNaN(customRate) && customRate > 0) {
        rate = customRate;
        rateSource = content.usingCustomRate;
      } else {
        // Fetch rate from API
        const response = await fetch(
          `https://open.er-api.com/v6/latest/${from}`
        );
        const data = await response.json();

        if (data.result === 'success' && data.rates && data.rates[to]) {
          rate = data.rates[to];
          rateSource = content.usingRealRateStatus;
        } else {
          throw new Error(content.errorFetching);
        }
      }

      const result = amount * rate;
      const symbol = currencySymbols[to] || '';
      resultElement.textContent = `${symbol}${result.toFixed(2)}`;
      rateUsedElement.textContent = `${rateSource}: 1 ${from} = ${rate.toFixed(4)} ${to}`;
    } catch (error) {
      console.error('Currency conversion error:', error);
      resultElement.textContent = content.convertError;
      rateUsedElement.textContent = content.networkError;
    }
  }

  async function showCurrentRate() {
    const from = fromSelect.value;
    const to = toSelect.value;

    if (from === to || !from || !to) {
      currentRateDisplay.textContent = '';
      return;
    }

    try {
      const response = await fetch(`https://open.er-api.com/v6/latest/${from}`);
      const data = await response.json();

      if (data.result === 'success' && data.rates && data.rates[to]) {
        const rate = data.rates[to];
        currentRateDisplay.textContent = `${content.currentRate}: 1 ${from} = ${rate.toFixed(4)} ${to}`;
      }
    } catch {
      currentRateDisplay.textContent = '';
    }
  }

  async function useRealRate() {
    const from = fromSelect.value;
    const to = toSelect.value;

    if (from === to) return;

    try {
      const response = await fetch(`https://open.er-api.com/v6/latest/${from}`);
      const data = await response.json();

      if (data.result === 'success' && data.rates && data.rates[to]) {
        customRateInput.value = data.rates[to].toFixed(4);
      }
    } catch (error) {
      console.error('Error fetching rate:', error);
    }
  }

  // Event listeners
  if (convertBtn) {
    convertBtn.addEventListener('click', convertCurrency);
  }

  if (refreshBtn) {
    refreshBtn.addEventListener('click', fetchRates);
  }

  if (useRealRateBtn) {
    useRealRateBtn.addEventListener('click', useRealRate);
  }

  if (fromSelect && toSelect) {
    fromSelect.addEventListener('change', showCurrentRate);
    toSelect.addEventListener('change', showCurrentRate);
  }

  // Update time display every minute
  setInterval(updateTimeDisplay, 60000);

  // Initial load
  fetchRates();
  showCurrentRate();
</script>
