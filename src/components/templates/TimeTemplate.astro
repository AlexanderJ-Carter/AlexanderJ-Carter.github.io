---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../PageHero.astro';

const { lang } = Astro.props;

type Lang = 'zh-CN' | 'zh-TW' | 'en-GB' | 'fr' | 'ru';

function getLangPath(path: string) {
  if (lang === 'zh-CN') return path;
  return `/${lang === 'en-GB' ? 'en' : lang}${path}`;
}

const timeData: Record<Lang, any> = {
  'zh-CN': {
    title: '世界时间',
    tag: '世界时区 · 标准时间',
    headerTitle: '世界时间',
    headerDesc:
      '一目了然的本地时间与常用时区，支持一键复制；开发者接口作为附加说明。',
    beijingLabel: '北京时间',
    beijingTz: 'UTC+8',
    yourTime: '你的本地时间',
    copy: '复制',
    copied: '已复制',
    worldClocks: '世界时区',
    london: '伦敦',
    newYork: '纽约',
    tokyo: '东京',
    sydney: '悉尼',
    utc: 'UTC',
    apiTitle: '可选开发者接口',
    apiDesc:
      '这是给前端调试、监控或小工具复用的 JSON 时间接口。普通访客只看上面的时钟即可；如果只提供当前时间，用 Cloudflare Worker 就够了，Tunnel 只适合暴露已有本地服务。',
    apiExample: 'GET https://api.alexander.xin/time/now',
    apiReturns: '返回 JSON：iso, timestamp, timezone, datetime',
    apiSetup: '接口说明',
    relatedTitle: '更多小工具',
    calendar: '日历',
    currency: '汇率',
    now: '此刻',
    uses: '本站',
    units: '单位换算',
    qr: 'QR 生成',
    back: '返回首页',
  },
  'zh-TW': {
    title: '世界時間',
    tag: '世界時區 · 標準時間',
    headerTitle: '世界時間',
    headerDesc:
      '一目了然的本地時間與常用時區，支援一鍵複製；開發者接口作為附加說明。',
    beijingLabel: '北京時間',
    beijingTz: 'UTC+8',
    yourTime: '你的本地時間',
    copy: '複製',
    copied: '已複製',
    worldClocks: '世界時區',
    london: '倫敦',
    newYork: '紐約',
    tokyo: '東京',
    sydney: '雪梨',
    utc: 'UTC',
    apiTitle: '可選開發者接口',
    apiDesc:
      '這是給前端調試、監控或小工具複用的 JSON 時間接口。普通訪客只看上面的時鐘即可；如果只提供目前時間，用 Cloudflare Worker 就夠了，Tunnel 只適合暴露已有本地服務。',
    apiExample: 'GET https://api.alexander.xin/time/now',
    apiReturns: '回傳 JSON：iso, timestamp, timezone, datetime',
    apiSetup: '接口說明',
    relatedTitle: '更多小工具',
    calendar: '日曆',
    currency: '匯率',
    now: '此刻',
    uses: '本站',
    units: '單位換算',
    qr: 'QR 生成',
    back: '返回首頁',
  },
  'en-GB': {
    title: 'World Time',
    tag: 'World clocks · Standard time',
    headerTitle: 'World Time',
    headerDesc:
      'Clear local and major time zones at a glance, with copy actions and an optional developer endpoint.',
    beijingLabel: 'Beijing',
    beijingTz: 'UTC+8',
    yourTime: 'Your local time',
    copy: 'Copy',
    copied: 'Copied',
    worldClocks: 'World clocks',
    london: 'London',
    newYork: 'New York',
    tokyo: 'Tokyo',
    sydney: 'Sydney',
    utc: 'UTC',
    apiTitle: 'Optional Developer Endpoint',
    apiDesc:
      'A small JSON time endpoint for frontend debugging, monitoring or utility reuse. Regular visitors can ignore it; use a Cloudflare Worker for current time, and Tunnel only when exposing an existing local service.',
    apiExample: 'GET https://api.alexander.xin/time/now',
    apiReturns: 'Returns JSON: iso, timestamp, timezone, datetime',
    apiSetup: 'Endpoint notes',
    relatedTitle: 'More tools',
    calendar: 'Calendar',
    currency: 'Currency',
    now: 'Now',
    uses: 'About site',
    units: 'Units',
    qr: 'QR Code',
    back: 'Back to home',
  },
  fr: {
    title: 'Heure mondiale',
    tag: 'Horloges mondiales · Heure standard',
    headerTitle: 'Heure mondiale',
    headerDesc:
      'Heure locale et fuseaux courants, copie et point d’accès développeur optionnel.',
    beijingLabel: 'Pékin',
    beijingTz: 'UTC+8',
    yourTime: 'Votre heure locale',
    copy: 'Copier',
    copied: 'Copié',
    worldClocks: 'Horloges mondiales',
    london: 'Londres',
    newYork: 'New York',
    tokyo: 'Tokyo',
    sydney: 'Sydney',
    utc: 'UTC',
    apiTitle: 'Point d’accès développeur optionnel',
    apiDesc:
      'Petit endpoint JSON pour debug frontend, monitoring ou réutilisation dans des outils. Un visiteur peut l’ignorer ; Worker suffit pour l’heure courante, Tunnel sert surtout à exposer un service local existant.',
    apiExample: 'GET https://api.alexander.xin/time/now',
    apiReturns: 'JSON : iso, timestamp, timezone, datetime',
    apiSetup: 'Notes endpoint',
    relatedTitle: "Plus d'outils",
    calendar: 'Calendrier',
    currency: 'Taux de change',
    now: 'Maintenant',
    uses: 'Le site',
    units: 'Unités',
    qr: 'QR Code',
    back: "Retour à l'accueil",
  },
  ru: {
    title: 'Мировое время',
    tag: 'Мировые часы · Стандартное время',
    headerTitle: 'Мировое время',
    headerDesc:
      'Местное и основные часовые пояса, копирование и необязательный endpoint для разработчиков.',
    beijingLabel: 'Пекин',
    beijingTz: 'UTC+8',
    yourTime: 'Ваше местное время',
    copy: 'Копировать',
    copied: 'Скопировано',
    worldClocks: 'Мировые часы',
    london: 'Лондон',
    newYork: 'Нью-Йорк',
    tokyo: 'Токио',
    sydney: 'Сидней',
    utc: 'UTC',
    apiTitle: 'Необязательный endpoint',
    apiDesc:
      'Небольшой JSON endpoint времени для отладки фронтенда, мониторинга или повторного использования в утилитах. Обычным посетителям он не нужен; Worker достаточно для текущего времени, Tunnel нужен только для существующего локального сервиса.',
    apiExample: 'GET https://api.alexander.xin/time/now',
    apiReturns: 'JSON: iso, timestamp, timezone, datetime',
    apiSetup: 'Заметки endpoint',
    relatedTitle: 'Ещё инструменты',
    calendar: 'Календарь',
    currency: 'Курс валют',
    now: 'Сейчас',
    uses: 'О сайте',
    units: 'Единицы',
    qr: 'QR-код',
    back: 'На главную',
  },
};

const content = timeData[lang as Lang] || timeData['en-GB'];

const worldZones = [
  { id: 'utc', label: content.utc, tz: 'UTC', utc: 'UTC+0', icon: 'UTC' },
  {
    id: 'london',
    label: content.london,
    tz: 'Europe/London',
    utc: 'UTC+0',
    icon: 'LON',
  },
  {
    id: 'ny',
    label: content.newYork,
    tz: 'America/New_York',
    utc: 'UTC−5',
    icon: 'NYC',
  },
  {
    id: 'tokyo',
    label: content.tokyo,
    tz: 'Asia/Tokyo',
    utc: 'UTC+9',
    icon: 'TYO',
  },
  {
    id: 'sydney',
    label: content.sydney,
    tz: 'Australia/Sydney',
    utc: 'UTC+11',
    icon: 'SYD',
  },
];

const relatedTools = [
  { href: getLangPath('/calendar'), label: content.calendar, icon: 'Cal' },
  { href: getLangPath('/currency'), label: content.currency, icon: 'FX' },
  { href: getLangPath('/now'), label: content.now, icon: 'Now' },
  { href: getLangPath('/uses'), label: content.uses, icon: '◇' },
  { href: getLangPath('/units'), label: content.units, icon: 'Unit' },
  { href: getLangPath('/qr'), label: content.qr, icon: 'QR' },
];
---

<BaseLayout title={content.title}>
  <PageHero
    folio="Folio Clock"
    kicker={content.tag}
    title={content.headerTitle}
    description={content.headerDesc}
    narrow={false}
  />

  <section class="py-12 md:py-16 container mx-auto px-4 lg:px-8">
    <div class="grid gap-8 lg:grid-cols-5 max-w-6xl mx-auto">
      <!-- Left: Main clock + Your time -->
      <div class="lg:col-span-3 space-y-6">
        <!-- Beijing / Primary clock -->
        <div
          class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-8 md:p-10 rounded-sm reveal"
        >
          <div class="relative flex flex-col md:flex-row md:items-start gap-6">
            <!-- 指针式钟表（北京时间） -->
            <div class="flex-shrink-0 mx-auto md:mx-0">
              <div class="relative w-32 h-32 sm:w-40 sm:h-40">
                <svg
                  viewBox="0 0 100 100"
                  class="w-full h-full drop-shadow-md"
                  role="img"
                  aria-hidden="true"
                >
                  <circle
                    cx="50"
                    cy="50"
                    r="48"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    class="text-[rgb(var(--color-border))]"></circle>
                  <circle
                    cx="50"
                    cy="50"
                    r="2"
                    fill="currentColor"
                    class="text-primary-500"></circle>
                  <!-- 12/3/6/9 刻度 -->
                  <line
                    x1="50"
                    y1="8"
                    x2="50"
                    y2="14"
                    stroke="currentColor"
                    stroke-width="2"
                    class="text-[rgb(var(--color-text-primary))]"></line>
                  <line
                    x1="86"
                    y1="50"
                    x2="92"
                    y2="50"
                    stroke="currentColor"
                    stroke-width="2"
                    class="text-[rgb(var(--color-text-primary))]"></line>
                  <line
                    x1="50"
                    y1="86"
                    x2="50"
                    y2="92"
                    stroke="currentColor"
                    stroke-width="2"
                    class="text-[rgb(var(--color-text-primary))]"></line>
                  <line
                    x1="8"
                    y1="50"
                    x2="14"
                    y2="50"
                    stroke="currentColor"
                    stroke-width="2"
                    class="text-[rgb(var(--color-text-primary))]"></line>
                  <!-- 时针 -->
                  <line
                    id="analog-hour"
                    x1="50"
                    y1="50"
                    x2="50"
                    y2="28"
                    stroke="currentColor"
                    stroke-width="3"
                    stroke-linecap="round"
                    class="text-[rgb(var(--color-text-primary))]"
                    transform="rotate(0 50 50)"></line>
                  <!-- 分针 -->
                  <line
                    id="analog-minute"
                    x1="50"
                    y1="50"
                    x2="50"
                    y2="18"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    class="text-primary-500"
                    transform="rotate(0 50 50)"></line>
                  <!-- 秒针 -->
                  <line
                    id="analog-second"
                    x1="50"
                    y1="50"
                    x2="50"
                    y2="14"
                    stroke="currentColor"
                    stroke-width="1"
                    stroke-linecap="round"
                    class="text-accent-500"
                    transform="rotate(0 50 50)"></line>
                </svg>
              </div>
            </div>
            <div class="flex-1 min-w-0">
              <div
                class="flex flex-wrap items-center justify-between gap-4 mb-6"
              >
                <div class="flex items-center gap-2">
                  <span class="text-2xl" aria-hidden="true">🇨🇳</span>
                  <div>
                    <p
                      class="text-sm font-medium text-[rgb(var(--color-text-secondary))] uppercase tracking-wider"
                    >
                      {content.beijingLabel}
                    </p>
                    <p
                      class="text-xs text-[rgb(var(--color-text-tertiary))] mt-0.5"
                    >
                      {content.beijingTz}
                    </p>
                  </div>
                </div>
                <button
                  type="button"
                  id="copy-main"
                  class="time-copy-btn inline-flex items-center gap-2 px-4 py-2 rounded-sm bg-[rgb(var(--color-bg-secondary))] hover:bg-primary-500/15 border border-[rgb(var(--color-border))] hover:border-primary-500/40 text-sm font-medium text-[rgb(var(--color-text-primary))] transition-colors focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2"
                  data-copy-label={content.copy}
                  data-copied-label={content.copied}
                >
                  <svg
                    class="w-4 h-4"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                    aria-hidden="true"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"
                    ></path>
                  </svg>
                  <span class="copy-btn-text">{content.copy}</span>
                </button>
              </div>
              <div
                class="text-5xl sm:text-6xl md:text-7xl font-mono font-bold tracking-tight text-[rgb(var(--color-text-primary))] tabular-nums py-4"
                id="main-clock"
              >
                00:00:00
              </div>
              <p
                class="text-lg text-[rgb(var(--color-text-secondary))]"
                id="main-date"
              >
                ----/--/--
              </p>
              <p
                class="text-sm text-[rgb(var(--color-text-tertiary))] mt-2 font-mono"
                id="main-utc"
              >
                UTC+8
              </p>
              <p
                class="text-xs text-[rgb(var(--color-text-tertiary))] mt-3 font-mono"
                id="main-unix"
              >
                Unix: --
              </p>
              <div
                class="mt-4 h-1 rounded-full bg-[rgb(var(--color-bg-secondary))] overflow-hidden"
                aria-hidden="true"
              >
                <div
                  id="main-clock-tick"
                  class="h-full bg-[rgb(var(--color-text-primary))] rounded-full transition-all duration-100"
                  style="width: 0%"
                >
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Your local time -->
        <div
          class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-6 rounded-sm reveal"
        >
          <div class="flex flex-wrap items-center justify-between gap-4">
            <p
              class="text-sm font-medium text-[rgb(var(--color-text-secondary))] flex items-center gap-2"
            >
              <span aria-hidden="true"></span>
              {content.yourTime}
            </p>
            <button
              type="button"
              id="copy-local"
              class="time-copy-btn inline-flex items-center gap-1.5 px-3 py-1.5 rounded-sm bg-[rgb(var(--color-bg-secondary))] hover:bg-primary-500/15 border border-[rgb(var(--color-border))] hover:border-primary-500/40 text-xs font-medium text-[rgb(var(--color-text-primary))] transition-colors focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2"
              data-copy-label={content.copy}
              data-copied-label={content.copied}
            >
              <svg
                class="w-3.5 h-3.5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                ><path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"
                ></path></svg
              >
              <span class="copy-btn-text">{content.copy}</span>
            </button>
          </div>
          <p
            class="text-3xl md:text-4xl font-mono font-bold tracking-tight tabular-nums text-[rgb(var(--color-text-primary))] mt-2"
            id="local-clock"
          >
            --:--:--
          </p>
          <p
            class="text-[rgb(var(--color-text-tertiary))] text-sm mt-1"
            id="local-date"
          >
            --
          </p>
        </div>
      </div>

      <!-- Right: World clocks + API -->
      <div class="lg:col-span-2 space-y-6">
        <div
          class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-6 rounded-sm reveal"
        >
          <h2
            class="text-lg font-heading font-bold mb-4 text-[rgb(var(--color-text-primary))]"
          >
            {content.worldClocks}
          </h2>
          <ul class="space-y-2" aria-label={content.worldClocks}>
            {
              worldZones.map((z) => (
                <li class="flex items-center justify-between gap-3 py-2.5 px-3 rounded-sm hover:bg-[rgb(var(--color-bg-accent))]/50 transition-colors">
                  <div class="flex items-center gap-3 min-w-0">
                    <span class="meta-mono text-xs tracking-wider text-primary-600 dark:text-primary-400 shrink-0" aria-hidden="true">
                      {z.icon}
                    </span>
                    <div class="min-w-0">
                      <span class="font-medium text-[rgb(var(--color-text-primary))]">
                        {z.label}
                      </span>
                      <span class="text-xs text-[rgb(var(--color-text-tertiary))] ml-2">
                        {z.utc}
                      </span>
                    </div>
                  </div>
                  <span
                    class="font-mono text-lg tabular-nums text-[rgb(var(--color-text-primary))] shrink-0"
                    id={`clock-${z.id}`}
                  >
                    --:--
                  </span>
                </li>
              ))
            }
          </ul>
        </div>

        <div
          class="border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-primary))] p-6 rounded-sm reveal"
        >
          <div class="flex items-center justify-between gap-2 mb-2">
            <h3
              class="text-base font-heading font-bold text-[rgb(var(--color-text-primary))]"
            >
              {content.apiTitle}
            </h3>
            <button
              type="button"
              id="copy-api-url"
              class="time-copy-btn inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-sm bg-primary-500/10 hover:bg-primary-500/20 text-primary-600 dark:text-primary-400 text-xs font-medium transition-colors focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2"
              data-copy-value="https://api.alexander.xin/time/now"
              data-copy-label={content.copy}
              data-copied-label={content.copied}
            >
              <svg
                class="w-3.5 h-3.5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                ><path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"
                ></path></svg
              >
              <span class="copy-btn-text">{content.copy}</span>
            </button>
          </div>
          <p class="text-sm text-[rgb(var(--color-text-secondary))] mb-3">
            {content.apiDesc}
          </p>
          <code
            class="block p-3 rounded-sm bg-[rgb(var(--color-bg-primary))]/80 dark:bg-black/20 text-xs font-mono break-all border border-[rgb(var(--color-border))] mb-2"
          >
            {content.apiExample}
          </code>
          {
            content.apiReturns && (
              <p class="text-xs text-[rgb(var(--color-text-tertiary))] mb-3">
                {content.apiReturns}
              </p>
            )
          }
        </div>
      </div>
    </div>

    <!-- Related tools -->
    <div
      class="max-w-6xl mx-auto mt-16 pt-12 border-t border-[rgb(var(--color-border))] reveal"
    >
      <p
        class="text-sm font-medium text-[rgb(var(--color-text-secondary))] mb-4"
      >
        {content.relatedTitle}
      </p>
      <nav class="flex flex-wrap gap-3" aria-label={content.relatedTitle}>
        {
          relatedTools.map((tool) => (
            <a
              href={tool.href}
              class="inline-flex items-center gap-2 px-4 py-2.5 rounded-sm bg-[rgb(var(--color-bg-secondary))] hover:bg-primary-500/10 border border-[rgb(var(--color-border))] hover:border-primary-500/30 text-[rgb(var(--color-text-primary))] font-medium transition-colors active:scale-[0.98]"
            >
              <span class="meta-mono text-xs tracking-wider text-primary-600 dark:text-primary-400" aria-hidden="true">{tool.icon}</span>
              {tool.label}
            </a>
          ))
        }
      </nav>
      <p class="mt-6">
        <a
          href={getLangPath('/')}
          class="link-underline text-primary-500 hover:text-primary-600 font-medium text-sm inline-flex items-center gap-1"
        >
          ← {content.back}
        </a>
      </p>
    </div>
  </section>
</BaseLayout>

<script is:inline define:vars={{ lang }}>
  const localeMap = {
    'zh-CN': 'zh-CN',
    'zh-TW': 'zh-TW',
    'en-GB': 'en-GB',
    fr: 'fr-FR',
    ru: 'ru-RU',
  };
  const locale = localeMap[lang] || 'en-GB';
  const zones = [
    { id: 'utc', tz: 'UTC' },
    { id: 'london', tz: 'Europe/London' },
    { id: 'ny', tz: 'America/New_York' },
    { id: 'tokyo', tz: 'Asia/Tokyo' },
    { id: 'sydney', tz: 'Australia/Sydney' },
  ];

  function updateClocks() {
    const now = new Date();
    const ms = now.getMilliseconds();

    const beijingTime = new Date(
      now.toLocaleString(locale, { timeZone: 'Asia/Shanghai' })
    );
    const mainClock = document.getElementById('main-clock');
    const mainDate = document.getElementById('main-date');
    const mainUtc = document.getElementById('main-utc');
    const mainUnix = document.getElementById('main-unix');
    const mainTick = document.getElementById('main-clock-tick');
    if (mainClock)
      mainClock.textContent = beijingTime.toLocaleTimeString(locale, {
        hour12: false,
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit',
      });
    if (mainDate)
      mainDate.textContent = beijingTime.toLocaleDateString(locale, {
        weekday: 'short',
        year: 'numeric',
        month: 'short',
        day: 'numeric',
      });
    if (mainUtc) mainUtc.textContent = 'UTC+8';
    if (mainUnix)
      mainUnix.textContent = 'Unix: ' + Math.floor(now.getTime() / 1000);
    if (mainTick) mainTick.style.width = (ms / 1000) * 100 + '%';

    const hourHand = document.getElementById('analog-hour');
    const minuteHand = document.getElementById('analog-minute');
    const secondHand = document.getElementById('analog-second');
    if (hourHand && minuteHand && secondHand) {
      const h =
        (beijingTime.getHours() % 12) +
        beijingTime.getMinutes() / 60 +
        beijingTime.getSeconds() / 3600;
      const m =
        beijingTime.getMinutes() +
        beijingTime.getSeconds() / 60 +
        beijingTime.getMilliseconds() / 60000;
      const s = beijingTime.getSeconds() + beijingTime.getMilliseconds() / 1000;
      hourHand.setAttribute('transform', `rotate(${h * 30} 50 50)`);
      minuteHand.setAttribute('transform', `rotate(${m * 6} 50 50)`);
      secondHand.setAttribute('transform', `rotate(${s * 6} 50 50)`);
    }

    const localClock = document.getElementById('local-clock');
    const localDate = document.getElementById('local-date');
    if (localClock)
      localClock.textContent = now.toLocaleTimeString(locale, {
        hour12: false,
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit',
      });
    if (localDate)
      localDate.textContent = now.toLocaleDateString(locale, {
        weekday: 'short',
        year: 'numeric',
        month: 'short',
        day: 'numeric',
      });

    const formatTime = (tz) =>
      new Date(now.toLocaleString(locale, { timeZone: tz })).toLocaleTimeString(
        locale,
        { hour: '2-digit', minute: '2-digit' }
      );
    zones.forEach((z) => {
      const el = document.getElementById('clock-' + z.id);
      if (el) el.textContent = formatTime(z.tz);
    });
  }

  setInterval(updateClocks, 100);
  updateClocks();

  function setupCopyBtn(btn, getText, successClass) {
    if (!btn) return;
    const labelEl = btn.querySelector('.copy-btn-text');
    const copyLabel = btn.getAttribute('data-copy-label') || 'Copy';
    const copiedLabel = btn.getAttribute('data-copied-label') || 'Copied';
    btn.addEventListener('click', async () => {
      const text =
        typeof getText === 'function'
          ? getText()
          : btn.getAttribute('data-copy-value') || '';
      try {
        await navigator.clipboard.writeText(text);
        if (labelEl) labelEl.textContent = copiedLabel;
        btn.classList.add(
          successClass || '!border-primary-500',
          '!bg-primary-500/20'
        );
        setTimeout(() => {
          if (labelEl) labelEl.textContent = copyLabel;
          btn.classList.remove(
            successClass || '!border-primary-500',
            '!bg-primary-500/20'
          );
        }, 2000);
      } catch {}
    });
  }

  setupCopyBtn(document.getElementById('copy-main'), function () {
    const mainClock = document.getElementById('main-clock');
    const mainDate = document.getElementById('main-date');
    return mainClock && mainDate
      ? mainClock.textContent + ' ' + mainDate.textContent
      : '';
  });
  setupCopyBtn(document.getElementById('copy-local'), function () {
    const localClock = document.getElementById('local-clock');
    const localDate = document.getElementById('local-date');
    return localClock && localDate
      ? localClock.textContent + ' ' + localDate.textContent
      : '';
  });
  setupCopyBtn(document.getElementById('copy-api-url'));
</script>
