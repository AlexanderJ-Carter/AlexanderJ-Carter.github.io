---
import TimelineTemplate from '../../components/templates/TimelineTemplate.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'en-US' } },
    { params: { lang: 'en-GB' } },
    { params: { lang: 'fr' } },
    { params: { lang: 'ru' } },
  ];
}

const { lang } = Astro.params;
---

<TimelineTemplate lang={lang} />
