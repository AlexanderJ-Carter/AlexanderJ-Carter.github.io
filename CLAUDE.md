# CLAUDE / Agent

`folio/` 主站；`gateway/` Pages 名片。

- 改站：`folio/src/`。勿删安全资产与 `.github/SECURITY.md`。勿提交密钥与 `instance/config.json`。
- 说明：[docs/FOLIO.md](./docs/FOLIO.md) · [folio/instance/README.md](./folio/instance/README.md)
- 动画尊重 `prefers-reduced-motion`；交互可键盘聚焦。
- 验证：`pnpm --dir folio build`；点验 `/` `/gallery` `/fun` `/updates` `/admin`。
- 未经明确要求不要 `git push`。
